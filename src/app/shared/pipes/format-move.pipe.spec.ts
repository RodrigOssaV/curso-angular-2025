import { FormatMovePipe } from './format-move.pipe';

describe('FormatMovePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMovePipe();
    expect(pipe).toBeTruthy();
  });
});
